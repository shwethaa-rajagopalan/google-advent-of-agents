# Google Cloud Project Configuration
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1

# Agent Engine Configuration (optional - will auto-create if not set)
# AGENT_ENGINE_RESOURCE_NAME=projects/YOUR_PROJECT/locations/us-central1/agentEngines/YOUR_ENGINE_ID

# Sandbox Configuration (REQUIRED for chart generation)
# Create a sandbox first: python manage_sandbox.py create
# Then set the resource name here:
SANDBOX_RESOURCE_NAME=

# Optional: Logging Level
LOG_LEVEL=INFO

# Optional: Model Configuration
# MODEL=gemini-3-flash-preview
# IMAGE_MODEL=gemini-3-pro-image-preview

# Optional: PDF Export (enabled by default)
# Set to false to generate HTML report only
# ENABLE_PDF_EXPORT=true
