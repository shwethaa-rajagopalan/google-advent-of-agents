node_modules/
dist/
.astro/
.vscode/
.git/
.dockerignore
Dockerfile
cloudbuild.yaml
# We want to INCLUDE .md files for the docs site build,
# so we override the root .dockerignore if it exists.
!**/*.md
!**/*.mdx
